# 更新日志

---

## v0.4.0 — UI 重构 + 提示升级 + 难度调整（2026-05-22）

### 新增

| 模块 | 说明 |
|------|------|
| 规则常驻提示卡 | 白底三栏卡片（🐴每种颜色1匹 / 📏每行每列1匹 / 🚫不能相邻），虚线分隔，始终可见 |
| 状态信息双卡 | 剩余小马（红字）+ 用时（蓝字），两个大卡片并排 |
| 底部操作栏重构 | 图标化：🗑清除(48px) / 🐴揭晓(60px) / 💡提示(60px) / 📍坐标(42px) |
| 顶部资源栏 | 体力⚡ + 爱心❤❤（带扣心闪烁）+ 🏠返回按钮 |
| 坐标显示 | 📍 按钮切换行列号标识 |
| 安全区适配 | `STATUS_BAR_HEIGHT` 自动获取，顶部偏移避开系统状态栏和胶囊按钮 |

### 修改

| 文件 | 变更 |
|------|------|
| `core/generator.js` | 难度重设（7×7/9×9删除 → 8×8/10×10/12×12），颜色面板全部替换为12色高饱和度方案 |
| `js/runtime/grid.js` | 岛屿式网格（圆角5px+3px间隙+无边框），全部UI方法重构，新增坐标渲染 |
| `js/main.js` | 生命值3→2，render调用链重写，新增坐标按钮处理 |
| `js/render.js` | 新增导出 `STATUS_BAR_HEIGHT` |
| `js/home.js` | 背景色改为 `#f1f2f4` |
| `github/pony-puzzle-plan.md` | v1.11 全文档同步 |

### 提示系统升级

| 改进 | 说明 |
|------|------|
| 逻辑推导引擎 | 不再读取 `hasPony`，基于已揭晓小马 + R1~R3 约束推导 |
| 跳过已标记 | Priority 3/4 只统计 HIDDEN 格子，不重复提示已标内容 |
| 单色锁定 | 某颜色只剩1个有效位置时提示揭晓或标记 |

### 难度配置

| 难度 | 网格 | 小马 | 概率 |
|------|------|------|------|
| 简单 | 8×8 | 8 | 40% |
| 中等 | 10×10 | 10 | 35% |
| 困难 | 12×12 | 12 | 25% |

### 颜色面板

12 色高饱和度方案（8 个用户指定 + 4 个补充），背景 `#f1f2f4`。

---

## v0.3.1 — 广告模块预留（2026-05-21）

### 新增文件

| 文件 | 说明 |
|------|------|
| `core/ad.js` | 激励视频广告模块（全部注释，开通流量主后启用） |

### 修改文件

| 文件 | 变更 |
|------|------|
| `js/main.js` | 预留广告调用入口（看广告体力 +1、看广告复活） |
| `core/stamina.js` | 新增 `adRecover()` 方法供广告回调使用 |

### 预留功能

- 首页「广告恢复体力」按钮 → `watchAdForStamina()`（看完后体力 +1）
- 生命归零时 → `watchAdForRevive()`（看完后复活 1 心）
- 启用方式：删 `core/ad.js` 头尾标记 + 换 `adUnitId` + 取消 `main.js` 3 处注释

---

## v0.3.0 — 首页 + 体力系统（2026-05-21）

### 新增文件

| 文件 | 说明 |
|------|------|
| `core/stamina.js` | 体力管理器：wx.Storage 持久化 + 消耗 + 10 分钟自然恢复 |
| `js/home.js` | 首页渲染器：体力显示 + 恢复倒计时 + 开始按钮 + 广告按钮 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `js/main.js` | 场景管理（home⇄game）+ 体力消费 + 弹窗按钮回首页 + 渲染循环常驻 |

### 新增功能

- 首页 UI：标题、副标题、体力条（5 格上限）、开始游戏按钮
- 体力系统：每局消耗 1 体力，每 10 分钟恢复 1 点，wx.Storage 持久化
- 体力不足时弹 Toast 提示，无法开始游戏
- 首页实时显示下次恢复倒计时（m:ss）
- 广告恢复体力按钮（预留，暂弹提示）
- 胜利/失败弹窗「再来一局」→ 优先检查体力 → 不够则回首页
- 渲染循环常驻（不再停止），首页和游戏共用同一帧循环

---

## v0.2.0 — 生命值 + 弹窗 + 颜色修复（2026-05-21）

### 修改文件

| 文件 | 变更 |
|------|------|
| `core/generator.js` | 颜色分配：Voronoi → 洪水填充，同色格子正交连通 |
| `js/main.js` | 新增生命值系统（3 心）、失败判定、计时器、弹窗按钮点击判断 |
| `js/runtime/grid.js` | 顶部状态栏（爱心 UI）、胜利/失败弹窗升级（用时+难度+数据）、弹窗按钮区域 |

### 新增功能

- 3 心生命值系统：猜错 −1 心，归零失败
- 顶部状态栏：爱心 + 🐴数 + ✘数
- 计时器：通关/失败时记录用时（m:ss）
- 胜利弹窗：显示难度、剩余生命、用时 → 「再来一局」
- 失败弹窗：显示已找到数、错误次数、用时 → 「重新开始」
- 弹窗按钮可点击：只有点按钮才重开，空白处无反应
- 弹窗状态下背后清除按钮自动屏蔽
- 颜色分配改为洪水填充：每种颜色正交连通一整块，零散点

---

## v0.1.0 — 核心玩法 MVP（2026-05-21）

### 新增文件

| 文件 | 说明 |
|------|------|
| `core/generator.js` | 关卡生成器：Voronoi 区划颜色分配 + 回溯算法放置小马 |
| `js/main.js` | 游戏主逻辑：触摸交互（点击/滑动/双击）+ 游戏循环 |
| `js/runtime/grid.js` | Canvas 网格渲染器：彩色格子 + 状态绘制 + 清除按钮 + 胜利弹窗 |
| `CHANGELOG.md` | 版本更新日志 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `game.js` | 入口改为导入 Main 类启动游戏 |
| `.eslintrc.js` | 补充小游戏全局变量声明 |
| `github/pony-puzzle-plan.md` | 同步难度配置（7×7/6、9×9/8）；文档版本 v1.7 |

### 已实现功能

- 三种难度随机分配（7×7=6匹 / 9×9=8匹 / 15×13=8~9匹）
- 4 大约束规则（行唯一、列唯一、3×3隔离、颜色唯一）的回溯求解
- 同色格子块状分布（Voronoi 种点区划）
- Canvas 彩色网格渲染（自适应屏幕尺寸）
- 单击标记灰色 X / 再次单击取消
- 滑动连续标记/取消（方向锁定：首格决定全程行为）
- 双击揭晓（有马出 🐴 / 无马出 ✘）
- 已揭晓格子锁定不可操作
- 一键清除所有标记按钮
- 胜利判定 + 通关弹窗 + 点击开始新局

---

> 项目仓库：`git@github.com:JCCHENCZ/Pony_Sudu_Game.git`
